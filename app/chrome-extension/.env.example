# Chrome Extension Public Key (base64-encoded)
# Copy this file to .env and replace with your actual manifest.key value
# This key is used to keep the extension ID stable across builds
CHROME_EXTENSION_KEY=YOUR_PUBLIC_KEY_HERE
